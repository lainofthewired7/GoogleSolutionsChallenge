version: "3.9"

services:
  db:
    image: postgis/postgis:15-3.3
    container_name: indicium-db
    environment:
      POSTGRES_USER: indicium
      POSTGRES_PASSWORD: indicium
      POSTGRES_DB: indicium
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  api:
    build: .
    container_name: indicium-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db
      - redis
    volumes:
      - ./dashboard/dist:/app/dashboard/dist

  redis:
    image: redis:7-alpine
    container_name: indicium-redis
    ports:
      - "6379:6379"
    restart: always

volumes:
  pgdata:
